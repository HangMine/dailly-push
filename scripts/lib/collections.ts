import { SECTION_CONFIG } from '../../src/lib/daily/constants';
import type { IssueCollection } from './types';

export const DEFAULT_COLLECTION: IssueCollection = {
  id: 'github-daily-3lists-qq',
  title: 'GitHub 天榜速递',
  sections: SECTION_CONFIG,
};
