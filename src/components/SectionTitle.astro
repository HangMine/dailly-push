---
interface Props {
  title: string;
  className?: string;
}

const { title, className = '' } = Astro.props;
---

<div class={`section-reveal ${className}`.trim()}>
  <h2 class="section-reveal__text">{title}</h2>
</div>
